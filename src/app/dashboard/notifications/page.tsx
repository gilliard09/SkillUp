'use client';import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';import {Bell,Check,Loader2} from 'lucide-react';
export default function Notifications(){const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data}=await supabase.from('notifications').select('*').eq('user_id',user.id).order('created_at',{ascending:false}).limit(50);setItems(data??[]);await supabase.from('notifications').update({read_at:new Date().toISOString()}).eq('user_id',user.id).is('read_at',null);setLoading(false)})()},[]);if(loading)return <div className="min-h-[60vh] flex items-center justify-center"><Loader2 className="animate-spin text-brand-primary"/></div>;return <div className="pb-24"><div className="flex gap-3 items-center mb-8"><Bell className="text-brand-primary"/><h1 className="text-3xl font-black text-white uppercase italic">Notificações</h1></div>{!items.length?<div className="p-10 rounded-3xl border border-dashed border-white/10 text-center"><Check className="mx-auto text-emerald-400"/><p className="text-white font-bold mt-3">Tudo em dia!</p><p className="text-slate-500 text-sm">Quando houver novidades, elas aparecerão aqui.</p></div>:<div className="space-y-3">{items.map(n=><div key={n.id} className="p-5 rounded-2xl bg-slate-900/60 border border-white/10"><p className="text-white font-black">{n.title}</p><p className="text-slate-400 text-sm mt-1">{n.message}</p></div>)}</div>}</div>}
